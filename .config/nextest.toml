[profile.default]
retries = { backoff = "exponential", count = 5, delay = "2s", max-delay = "30s", jitter = true }
fail-fast = false

[[profile.default.overrides]]
filter = 'test(test_hadoop_iceberg_provider_includes_tables)'
slow-timeout = { period = "30s", terminate-after = 5 }