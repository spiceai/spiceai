version: v2
kind: Spicepod
name: auth_test_combined

runtime:
  auth:
    oidc:
      enabled: true
      issuer_url: https://login.microsoftonline.com/${AZURE_TENANT_ID}/v2.0
      audience:
        - api://6a000e68-c1ce-4f22-ad03-a92cd42792a6
    api_key:
      enabled: true
      keys:
        - "${SPICE_API_KEY}"
