version: v1
kind: Spicepod
name: iceberg[hadoop]-federated

datasets:
  - from: iceberg:s3a://benchmarks/tpch/customer
    name: customer
    params: &hadoop_params
      iceberg_s3_endpoint: ${secrets:S3_ENDPOINT}
      iceberg_s3_access_key_id: ${secrets:S3_KEY}
      iceberg_s3_secret_access_key: ${secrets:S3_SECRET}
      iceberg_s3_region: us-east-1
  - from: iceberg:s3a://benchmarks/tpch/lineitem
    name: lineitem
    params: *hadoop_params
  - from: iceberg:s3a://benchmarks/tpch/nation
    name: nation
    params: *hadoop_params
  - from: iceberg:s3a://benchmarks/tpch/orders
    name: orders
    params: *hadoop_params
  - from: iceberg:s3a://benchmarks/tpch/part
    name: part
    params: *hadoop_params
  - from: iceberg:s3a://benchmarks/tpch/partsupp
    name: partsupp
    params: *hadoop_params
  - from: iceberg:s3a://benchmarks/tpch/region
    name: region
    params: *hadoop_params
  - from: iceberg:s3a://benchmarks/tpch/supplier
    name: supplier
    params: *hadoop_params
