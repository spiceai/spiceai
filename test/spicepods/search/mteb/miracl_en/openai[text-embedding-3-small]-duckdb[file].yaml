version: v1
kind: Spicepod
name: openai[text-embedding-3-small]-duckdb[file]
datasets:

  # MTEB MIRACL English Retrieval dataset files (*.parquet) are automatically downloaded by the test operator.
  # https://huggingface.co/datasets/mteb/MIRACLRetrieval_en_top_250_only_w_correct-v2/tree/main

  - from: file:./corpus.parquet
    name: corpus
    acceleration:
      enabled: true
      engine: duckdb
      mode: file
    columns:
      - name: text
        embeddings:
          - from: openai_embed
            row_id:
              - _id

  - from: file:./queries.parquet
    name: test_queries
    acceleration:
      enabled: true

  - from: file:./data.parquet
    name: relevance_data
    acceleration:
      enabled: true

embeddings:
  - from: openai:text-embedding-3-small
    name: openai_embed
    params:
