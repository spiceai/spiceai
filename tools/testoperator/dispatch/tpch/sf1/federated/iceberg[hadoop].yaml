tests:
  bench:
    spicepod_path: federated/iceberg[hadoop].yaml
    query_set: tpch
    runner_type: spiceai-dev-runners
    # TPCH Q6 fails: Spark schema inference from the data load casts numerics
    # wrong. Needs schema changes before this arm can validate.
    validate_results: false
