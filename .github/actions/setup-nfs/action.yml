name: 'Setup NFS Dependencies'
description: 'Install libnfs development libraries for building and linking against NFS'
runs:
  using: 'composite'
  steps:
    - name: Install libnfs development libraries
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y libnfs-dev
