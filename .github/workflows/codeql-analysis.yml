name: 'CodeQL'

# Rust CodeQL security scan. GitHub's automatic Code Quality job covers
# interpreted languages on pull requests (e.g. Analyze (python)); compiled
# Rust needs this autobuild. The trunk ruleset requires CodeQL results on the
# PR head. Merge-queue SHAs still run Analyze as a check; SARIF is not
# published there because the queue ref does not survive until upload.
#
# `pull_request` loads this file from the PR, so a pull request can replace
# the scan with a clean SARIF. That event is not used. `pull_request_target`
# loads it from the default branch instead and checks out the PR head. Do
# not add paths-ignore on `pull_request_target`: a skipped rust scan leaves
# the enterprise code-scanning rule waiting on a check that never starts.
#
# `merge_group` loads this file from the merge commit and checks out that
# commit (`github.sha`) so the Analyze check runs on the queue SHA. SARIF
# is not published for that event: the `gh-readonly-queue/*` ref is deleted
# before the default-branch uploader starts. Code scanning is published from
# `pull_request_target` against `refs/pull/<n>/head`. Do not add
# `allow-unsafe-pr-checkout`: fork heads stay fail-closed.
#
# This workflow is read-only. The Analyze job never writes SARIF: a Cargo
# build script shares that VM and can replace files under `runner.temp`
# before `upload-artifact`. It uploads the finalized CodeQL database.
# `Produce SARIF` runs on a fresh VM, analyzes that database, and publishes
# the SARIF artifact. `codeql-upload.yml` (`workflow_run`, default branch)
# is what calls `upload-sarif`.
on:
  workflow_dispatch:
  pull_request_target:
    branches:
      - trunk
      - release-*
      - release/*
  merge_group:
    branches:
      - trunk
      - release-*
      - release/*
  push:
    branches:
      - trunk
      - release-*
      - release/*
    paths-ignore:
      - 'docs/**'
      - '.github/**'
  schedule:
    - cron: '19 18 * * 4'

# pull_request_target still checks out the PR head below. The token stays
# read-only and the job must not request security-events: write.
permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    # The checked-out tree is untrusted on pull_request_target, merge_group,
    # and a workflow_dispatch of a feature branch. Cargo build scripts and
    # local composite actions must not run on the shared self-hosted pool or
    # receive MinIO credentials. push and schedule already only run on
    # trunk and release branches. workflow_dispatch gets that pool only on
    # those refs; a dispatch of any other branch stays on the hosted runner
    # without secrets. merge_group is hosted and secret-less for the same
    # reason.
    runs-on: ${{ (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.ref == 'refs/heads/trunk' || startsWith(github.ref, 'refs/heads/release-') || startsWith(github.ref, 'refs/heads/release/')))) && 'spiceai-dev-runners' || 'ubuntu-24.04' }}
    env:
      HAS_TEST_MINIO_SECRET: ${{ (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.ref == 'refs/heads/trunk' || startsWith(github.ref, 'refs/heads/release-') || startsWith(github.ref, 'refs/heads/release/')))) && secrets.TEST_MINIO_ENDPOINT != '' && secrets.TEST_MINIO_ACCESS_KEY != '' && secrets.TEST_MINIO_SECRET_KEY != '' }}
      # pull_request_target, merge_group, and a feature-branch dispatch check
      # out an untrusted tree, then run local composite actions. Those Actions
      # files are overlaid from a trusted revision after checkout so a PR
      # cannot replace setup-make / setup-cc / configure-apt.
      OVERLAY_TRUSTED_ACTIONS: ${{ github.event_name == 'pull_request_target' || github.event_name == 'merge_group' || (github.event_name == 'workflow_dispatch' && github.ref != 'refs/heads/trunk' && !startsWith(github.ref, 'refs/heads/release-') && !startsWith(github.ref, 'refs/heads/release/')) }}
    # An unbounded autobuild waits out GitHub's six-hour default (#12717).
    # Hosted uncached rust analysis has been completing in about 80 minutes;
    # the bound sits above that.
    timeout-minutes: 150
    permissions:
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: ['rust']
        # CodeQL supports [ 'cpp', 'csharp', 'go', 'java', 'javascript', 'python', 'rust' ]
        # Learn more:
        # https://docs.github.com/en/free-pro-team@latest/github/finding-security-vulnerabilities-and-errors-in-your-code/configuring-code-scanning#changing-the-languages-that-are-analyzed

    steps:
      # push, schedule, workflow_dispatch, and merge_group scan github.sha.
      # merge_group's github.sha is the queue merge commit. pull_request_target's
      # github.sha is the base branch; the scan has to be the PR head. Fork
      # heads are not fetched: allow-unsafe-pr-checkout stays unset.
      - name: Checkout repository
        if: github.event_name != 'pull_request_target'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Checkout pull request head
        if: github.event_name == 'pull_request_target'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha }}

      # Nested `uses: ./.github/actions/configure-apt` resolves from the
      # workspace root, not from the composite file. Overlay the whole
      # Actions directory from the PR/merge base (or the default branch on
      # an untrusted dispatch) so those calls cannot load the checked-out
      # tree's copies.
      - name: Checkout trusted Actions definitions
        if: env.OVERLAY_TRUSTED_ACTIONS == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ github.event_name == 'pull_request_target' && github.sha || github.event.merge_group.base_sha || github.event.repository.default_branch }}
          path: .trusted-ci
          sparse-checkout: |
            .github/actions
          sparse-checkout-cone-mode: true

      - name: Restore trusted Actions definitions
        if: env.OVERLAY_TRUSTED_ACTIONS == 'true'
        shell: bash
        run: |
          set -euo pipefail
          trusted='.trusted-ci/.github/actions'
          if [[ ! -d "${trusted}" ]]; then
            echo "::error::Trusted Actions definitions were not checked out, so the untrusted tree's local actions will not run."
            exit 1
          fi
          rm -rf .github/actions
          mkdir -p .github
          cp -a "${trusted}" .github/actions
          rm -rf .trusted-ci

      - name: Set up sccache with local MinIO
        if: env.HAS_TEST_MINIO_SECRET == 'true'
        uses: ./.github/actions/setup-sccache
        with:
          minio_endpoint: ${{ secrets.TEST_MINIO_ENDPOINT }}
          minio_access_key: ${{ secrets.TEST_MINIO_ACCESS_KEY }}
          minio_secret_key: ${{ secrets.TEST_MINIO_SECRET_KEY }}

      # Initializes the CodeQL tools for scanning.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4
        with:
          languages: ${{ matrix.language }}
          db-location: ${{ runner.temp }}/codeql_databases
          # If you wish to specify custom queries, you can do so here or in a config file.
          # By default, queries listed here will override any specified in a config file.
          # Prefix the list here with "+" to use these queries and those in the config file.
          # queries: ./path/to/local/query, your-org/your-repo/queries@main

      - name: Set up make
        if: runner.os != 'macOS'
        uses: ./.github/actions/setup-make

      - name: Set up cc
        if: runner.os != 'macOS'
        uses: ./.github/actions/setup-cc

      # Autobuild attempts to build any compiled languages  (C/C++, C#, or Java).
      # If this step fails, then you should remove it and run the build manually (see below)
      - name: Autobuild
        uses: github/codeql-action/autobuild@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4

      # ℹ️ Command-line programs to run using the OS shell.
      # 📚 https://git.io/JvXDl

      # ✏️ If the Autobuild fails above, remove it and uncomment the following three lines
      #    and modify them (or add more) to build your code if your project
      #    uses a compiled language

      #- run: |
      #   make bootstrap
      #   make release

      - name: Show sccache stats
        if: always() && (env.HAS_TEST_MINIO_SECRET == 'true')
        # `always()`: this is the one step that reports cache health, and without it
        # it is skipped on exactly the failures it would explain (#12420). The `||`
        # keeps a diagnostic from ever deciding a verdict — when the cache backend is
        # gone `--show-stats` cannot answer either, and that is itself the finding.
        run: |
          sccache --show-stats || echo "::warning::sccache --show-stats failed, so the cache server is not answering either. Read a build failure in this job as a possible cache-backend outage rather than a code failure."

      # Finalize the database on this VM but do not run queries. Query output
      # would land under runner.temp on the same user as the Cargo build.
      - name: Finalize CodeQL database
        uses: github/codeql-action/analyze@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4
        with:
          skip-queries: true
          upload: never
          upload-database: false
          output: ${{ runner.temp }}/codeql-sarif-unused

      - name: Upload CodeQL database
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: codeql-database-${{ matrix.language }}
          path: ${{ runner.temp }}/codeql_databases
          if-no-files-found: error
          # Databases are large; only the follow-up SARIF job needs them.
          retention-days: 1
          # A full-job rerun reaches this step again in the same workflow run.
          # Artifact names are immutable unless overwrite is set, and the
          # default (`false`) fails that retry with HTTP 409.
          overwrite: true

  sarif:
    name: Produce SARIF
    needs: analyze
    runs-on: ubuntu-24.04
    # Query evaluation against an already-built Rust database, not an
    # autobuild. Anything past this is wedged, not a cold cache.
    timeout-minutes: 60
    permissions:
      actions: read
      contents: read
    steps:
      - name: Download CodeQL database
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: codeql-database-*
          path: ${{ runner.temp }}/codeql-imported
          merge-multiple: true

      # Installs the CLI only. This job never checks out the scanned tree
      # and never runs a Cargo build, so a process left by Analyze cannot
      # reach the SARIF it produces.
      - name: Setup CodeQL CLI
        id: setup-codeql
        uses: github/codeql-action/setup-codeql@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4
        with:
          languages: rust

      - name: Analyze imported database
        env:
          CODEQL_PATH: ${{ steps.setup-codeql.outputs.codeql-path }}
          IMPORTED_ROOT: ${{ runner.temp }}/codeql-imported
          SARIF_DIR: ${{ runner.temp }}/codeql-sarif
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "${CODEQL_PATH}" || ! -x "${CODEQL_PATH}" ]]; then
            echo "::error::CodeQL CLI was not installed, so SARIF was not produced."
            exit 1
          fi
          mkdir -p "${SARIF_DIR}"
          found=0
          shopt -s nullglob
          for db in "${IMPORTED_ROOT}"/*/; do
            if [[ ! -f "${db}codeql-database.yml" ]]; then
              continue
            fi
            lang="$(basename "${db}")"
            found=1
            "${CODEQL_PATH}" database analyze "${db}" \
              --format=sarifv2.1.0 \
              --output="${SARIF_DIR}/${lang}.sarif"
          done
          if [[ "${found}" -eq 0 ]]; then
            echo "::error::No CodeQL database was found in the imported artifact, so SARIF was not produced."
            exit 1
          fi

      - name: Upload SARIF artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: codeql-sarif-rust
          path: ${{ runner.temp }}/codeql-sarif
          if-no-files-found: error
          # A full-job rerun reaches this step again in the same workflow run.
          overwrite: true
