# Spice v2.0.0-rc.1 (Mar 2, 2026)

v2.0.0-rc.1 is the first release candidate for early testing of v2.0 features including **Active-Active Highly-Available Distributed Query** object-store native and built on Apache Ballista with partition management, dynamic cluster sizing, and cluster observability, **Spice Cayenne GA** with staged append writes, file-based retention deletes, composite partitioning, and distributed ingestion, **DataFusion v52.2.0 Upgrade** with sort pushdown, new merge join, and dynamic filters, **Iceberg DDL Support** for `CREATE TABLE` and `DROP TABLE` via SQL, **DuckLake Catalog & Data Connector** for lakehouse-style data management, **GCS Data Connector (Alpha)** for Google Cloud Storage, **Rust CLI Rewrite** for a unified single-binary experience, and **DuckDB v1.4.4**, **delta_kernel v0.18.2**, and **mistral.rs** upgrades.

## What's New in v2.0.0-rc.1

### Active-Active HA Distributed Query

[Distributed Query](https://spiceai.org/docs/features/distributed-query) exits Beta with active-active highly-available object-store-based distributed query:

- **Partition Management**: New `PartitionManagementTask` enables schedulers to dynamically assign and update accelerated table partitions across executors
- **Dynamic Cluster Sizing**: `target_partitions` is now set dynamically based on cluster capacity instead of using static optimizer rules
- **Local Execution in Cluster Mode**: `/v1/sql` and FlightSQL queries now execute locally on the scheduler when appropriate, reducing inter-node overhead
- **UDTF Serialization**: User-Defined Table Functions are now serialized for distributed Ballista execution
- **Executor Lifecycle**: Executors deregister on shutdown; heartbeat timeout reduced from 180s to 30s for faster failure detection
- **Cluster Observability**: New metrics and Grafana dashboard for monitoring distributed query clusters

### Spice Cayenne Improvements

The [Spice Cayenne data accelerator](https://spiceai.org/docs/components/data-accelerators/cayenne) exits Beta with significant reliability and performance improvements:

- **Staged Append Writes**: WAL-based staged append writes prevent partial writes and data loss on stream errors. Batches are written to a WAL file before being committed, ensuring atomicity.
- **File-Based Retention Deletes**: Time-based retention now supports file-level deletes for both position-based and primary-key tables, reducing I/O overhead compared to row-level deletion.
- **Multiple Partition Expressions**: Support for composite partitioning with `partition_by: [col1, col2]` using hierarchical path-like keys (e.g., `2025/10/15`).
- **Distributed Ingestion**: Cayenne catalog now supports distributed ingestion across executor nodes in cluster mode, including `UPDATE` operations.
- **Improved Robustness**: Fixed CDC edge case where `DELETE` + `UPSERT` sequences could produce duplicate primary keys across protected snapshots. Improved upsert handling during runtime restarts.

### DataFusion v52.2.0 Upgrade

[Apache DataFusion has been upgraded to v52.2.0](https://datafusion.apache.org/blog/output/2026/01/12/datafusion-52.0.0/), bringing significant performance improvements, new query features, and enhanced extensibility.

**Performance Improvements**:

- **Faster `CASE` Expressions**: Lookup-table-based evaluation for certain `CASE` expressions avoids repeated evaluation, accelerating common ETL patterns
- **`MIN`/`MAX` Aggregate Dynamic Filters**: Queries with `MIN`/`MAX` aggregates now create dynamic filters during scan to prune files and rows as tighter bounds are discovered during execution
- **New Merge Join**: Rewritten sort-merge join (SMJ) operator with speedups of three orders of magnitude in pathological cases (e.g., TPC-H Q21: minutes → milliseconds)
- **Caching Improvements**: New statistics cache for file metadata avoids repeatedly recalculating statistics, significantly improving planning time. A prefix-aware list-files cache accelerates evaluating partition predicates for Hive partitioned tables
- **Improved Hash Join Filter Pushdown**: Build-side hash map contents are now passed dynamically to probe-side scans for pruning files, row groups, and individual rows

**Major Features**:

- **Sort Pushdown to Scans**: Sorts are pushed into data sources, enabling ~30x performance improvement on pre-sorted data with top-K queries. Parquet scans now reverse row group order for `DESC` queries on `ASC`-sorted files
- **`TableProvider` supports `DELETE` and `UPDATE`**: New hooks for `DELETE` and `UPDATE` statements in the `TableProvider` trait, enabling downstream storage engines to plug in mutation logic
- **Arrow IPC Stream File Support**: DataFusion can now read Arrow IPC stream files, expanding interoperability with systems that emit Arrow streams directly
- **More Extensible SQL Planning**: New `RelationPlanner` API for extending SQL planning for `FROM` clauses, enabling support for vendor-specific SQL dialects
- **Expression Evaluation Pushdown to Scans**: Predicates and expressions can now be customized per individual file schema via `PhysicalExprAdapter`
- **`CoalesceBatchesExec` Removed**: Coalescing integrated directly into operators (filters, joins, repartitions), reducing plan complexity while keeping batch sizes efficient

### Iceberg DDL Support

**SQL Schema Management**: Spice now supports `CREATE TABLE` and `DROP TABLE` DDL operations for [Iceberg](https://iceberg.apache.org/) catalogs via FlightSQL and the `/v1/sql` API. A new `ComposedCatalogProvider` wraps external catalogs with Spice's internal schemas, and DML validation has been updated for catalog-level writability.

### DuckLake Catalog & Data Connector

**Lakehouse-Style Data Management**: New [DuckLake](https://ducklake.select/) catalog and data connector enables lakehouse-style data management with DuckDB as the metadata catalog and object storage for data files. DuckLake provides ACID transactions, time travel, and schema evolution on top of Parquet files.

### GCS Data Connector (Alpha)

**Google Cloud Storage Support**: New [Google Cloud Storage](https://cloud.google.com/storage) data connector enables federated queries against data stored in GCS buckets, with Iceberg table support.

### Rust CLI Rewrite

**Unified Single-Binary Experience**: The Spice CLI has been completely rewritten from Go to Rust, eliminating the Go dependency and providing a single `spice` binary built from the same codebase as `spiced`. This improves startup performance, reduces distribution size, and ensures consistent behavior between CLI and runtime.

**Key Features**:

- **Full Feature Parity**: All 27+ CLI commands re-implemented in Rust with identical behavior
- **New `spice query` Command**: Interactive REPL for [async queries](https://spiceai.org/docs/features/query-federation/async-queries) via the `/v1/queries` API with multi-line SQL input, spinner progress indicator, Ctrl+C cancellation, and partial query ID matching
- **`--output=json` Flag**: Machine-readable JSON output for CLI commands, enabling scripting and automation
- **`spice login --output`**: New output modes (`env`, `json`, `keychain`) for flexible credential management
- **`spice cloud metrics`**: New command for Spice Cloud deployment metrics

### Models Included by Default

Local LLM/ML model inference (via `mistral.rs`) is now included in the default Spice build. The separate `models` build variant has been removed. This simplifies installation and ensures all users have access to local AI inference capabilities.

### Additional Dependency Upgrades

| Dependency         | Version                                                              |
| ------------------ | -------------------------------------------------------------------- |
| **DuckDB**         | v1.4.4                                                               |
| **delta_kernel**   | v0.18.2                                                              |
| **mistral.rs**     | Upgraded (candle fork removed, now uses candle 0.9.2 from crates.io) |
| **Turso (libsql)** | v0.4.4                                                               |
| **Vortex**         | Upgraded with CASE-WHEN support                                      |
| **AWS SDK**        | Multiple crates updated + APN user-agent support                     |

### Other Improvements

- **x.ai Models**: x.ai models now exclusively use the `/v1/responses` endpoint with rate limiting support.
- **HuggingFace Chat Templates**: Added support for chat templates in HuggingFace model configurations.
- **Databricks SQL Dialect**: Added Databricks SQL dialect for DataFusion unparser, improving federation query generation.
- **Snowflake**: Added `snowflake_private_key` parameter for key-pair authentication.
- **Acceleration Metrics**: New `rows_written`, `bytes_written`, and `dataset_acceleration_size_bytes` metrics for acceleration refresh ingestion.
- **Refresh SQL UDFs**: Core scalar UDFs are now enabled in refresh SQL expressions.
- **FlightSQL**: Fixed TLS connection handling for `grpc+tls://` endpoints with custom CA certificate support.
- **FlightSQL**: Fixed schema consistency by expanding view types and verifying field names.
- **Hash Index**: Fixed query correctness when hash index is used with additional filters.
- **Results Cache**: Fixed schema preservation for empty query results.
- **Query Nullability**: Reconciled execution stream nullability with logical plan schema.
- **Schema Evolution**: Graceful handling of schema evolution mismatch errors during data refresh.
- **Internal YAML Parser**: Replaced deprecated `serde_yaml` with an internal YAML implementation.
- **Data Connector Crates**: Data connectors split into separate crates for faster incremental builds.

## Contributors

- [@avishafir](https://github.com/avishafir)
- [@cluster2600](https://github.com/cluster2600)
- [@krinart](https://github.com/krinart)
- [@lukekim](https://github.com/lukekim)
- [@peasee](https://github.com/peasee)
- [@phillipleblanc](https://github.com/phillipleblanc)
- [@sgrebnov](https://github.com/sgrebnov)

## Breaking Changes

- **Cayenne and Distributed Query exit Beta**: Beta warnings have been removed from documentation and code. Both features are now considered GA-ready.
- **Models included by default**: The separate `models` build variant has been removed. Local LLM inference is now always included.
- **Windows native builds removed**: Native Windows builds are no longer provided. Use [WSL](https://learn.microsoft.com/en-us/windows/wsl/) instead.
- **Metric renames**: `accelerated_refresh` metrics renamed to `acceleration_refresh` for consistency. `last_refresh_time` gauge renamed to include milliseconds unit.
- **Caching config renamed**: `ResultsCache` replaced with `SQLResultsCacheConfig` in configuration.
- **DuckDB parameter rename**: `partitioned_write_flush_threshold` renamed to `partitioned_write_flush_threshold_rows`.
- **v1/search API**: The `/v1/search` API now always returns an array in `matches`, even for single results.
- **x.ai model endpoint**: x.ai models now exclusively use the `/v1/responses` endpoint.
- **Error messages**: Error messages across S3 Vectors, ScyllaDB, Snowflake, ClickHouse, and other components have been refactored for clarity and consistency.

## Cookbook Updates

No major cookbook updates. The [Spice Cookbook](https://spiceai.org/cookbook) includes 86 recipes to help you get started with Spice quickly and easily.

## Upgrading

To upgrade to v2.0.0-rc.1, use one of the following methods:

**CLI**:

```console
spice upgrade
```

**Homebrew**:

```console
brew upgrade spiceai/spiceai/spice
```

**Docker**:

Pull the `spiceai/spiceai:2.0.0-rc.1` image:

```console
docker pull spiceai/spiceai:2.0.0-rc.1
```

For available tags, see [DockerHub](https://hub.docker.com/r/spiceai/spiceai/tags).

**Helm**:

```console
helm repo update
helm upgrade spiceai spiceai/spiceai --version 2.0.0-rc.1
```

**AWS Marketplace**:

Spice is available in the [AWS Marketplace](https://aws.amazon.com/marketplace/pp/prodview-jmf6jskjvnq7i).

## What's Changed

### Changelog

- Rewrite Go CLI in Rust by [@peasee](https://github.com/peasee) in [#9061](https://github.com/spiceai/spiceai/pull/9061)
- feat: Add `spice query` command for async queries REPL by [@peasee](https://github.com/peasee) in [#9057](https://github.com/spiceai/spiceai/pull/9057)
- add `--output=json` field to CLI by [@peasee](https://github.com/peasee) in [#9469](https://github.com/spiceai/spiceai/pull/9469)
- Add `--output` flag to `spice login` with env/json/keychain modes by [@peasee](https://github.com/peasee) in [#9541](https://github.com/spiceai/spiceai/pull/9541)
- initial `spice-cloud-client` crate and `spice cloud metrics --app <app-name>` by [@lukekim](https://github.com/lukekim) in [#9480](https://github.com/spiceai/spiceai/pull/9480)
- Upgrade to DataFusion v52.2.0 by [@phillipleblanc](https://github.com/phillipleblanc) in [#9419](https://github.com/spiceai/spiceai/pull/9419)
- v2.0 breaking changes by [@phillipleblanc](https://github.com/phillipleblanc) in [#9233](https://github.com/spiceai/spiceai/pull/9233)
- Initial implementation of DuckLake catalog & data connectors by [@phillipleblanc](https://github.com/phillipleblanc) in [#9083](https://github.com/spiceai/spiceai/pull/9083)
- Add DuckLake TPCH E2E workflow and federated Spicepod configuration by [@phillipleblanc](https://github.com/phillipleblanc) in [#9431](https://github.com/spiceai/spiceai/pull/9431)
- feat: Add Iceberg DDL support (CREATE TABLE / DROP TABLE) for default catalog override by [@phillipleblanc](https://github.com/phillipleblanc) in [#9440](https://github.com/spiceai/spiceai/pull/9440)
- GCS Data Connector (Alpha) by [@phillipleblanc](https://github.com/phillipleblanc) in [#9084](https://github.com/spiceai/spiceai/pull/9084)
- feat: Split data connectors into separate crates by [@phillipleblanc](https://github.com/phillipleblanc) in [#8936](https://github.com/spiceai/spiceai/pull/8936)
- Remove models variant (now default) & Windows builds (use WSL) by [@phillipleblanc](https://github.com/phillipleblanc) in [#9063](https://github.com/spiceai/spiceai/pull/9063)
- feat(runtime-api-types): Add shared API types for runtime HTTP & CLI by [@peasee](https://github.com/peasee) in [#9194](https://github.com/spiceai/spiceai/pull/9194)
- cayenne: Staged append writes to prevent partial writes and data loss on stream error by [@sgrebnov](https://github.com/sgrebnov) in [#9491](https://github.com/spiceai/spiceai/pull/9491)
- fix(cayenne): stream PK retention deletes and run OOM regression in CI by [@sgrebnov](https://github.com/sgrebnov) in [#9533](https://github.com/spiceai/spiceai/pull/9533)
- feat(cayenne): File-based retention deletes for PK tables by [@sgrebnov](https://github.com/sgrebnov) in [#9387](https://github.com/spiceai/spiceai/pull/9387)
- Cayenne: add file-based deletes for time retention (position-based only) by [@sgrebnov](https://github.com/sgrebnov) in [#9366](https://github.com/spiceai/spiceai/pull/9366)
- Cayenne: Add time-based retention filtering at scan time by [@sgrebnov](https://github.com/sgrebnov) in [#9362](https://github.com/spiceai/spiceai/pull/9362)
- feat: Support multiple `partition_by` expressions for Cayenne by [@sgrebnov](https://github.com/sgrebnov) in [#9241](https://github.com/spiceai/spiceai/pull/9241)
- Support multiple partition expressions in partition_by configuration by [@sgrebnov](https://github.com/sgrebnov) in [#9201](https://github.com/spiceai/spiceai/pull/9201)
- Fix Cayenne duplicate primary keys after DELETE + UPSERT CDC sequences by [@krinart](https://github.com/krinart) in [#9494](https://github.com/spiceai/spiceai/pull/9494)
- Improve cayenne to handle upserts more robust during runtime restarts by [@krinart](https://github.com/krinart) in [#9350](https://github.com/spiceai/spiceai/pull/9350)
- Cayenne: share upload semaphore across partitions to bound memory growth and optimize I/O by [@sgrebnov](https://github.com/sgrebnov) in [#9078](https://github.com/spiceai/spiceai/pull/9078)
- Cayenne: Allow append mode with both primary_key and time_column by [@sgrebnov](https://github.com/sgrebnov) in [#9090](https://github.com/spiceai/spiceai/pull/9090)
- feat: Support distributed ingestion in cayenne catalog by [@phillipleblanc](https://github.com/phillipleblanc) in [#9506](https://github.com/spiceai/spiceai/pull/9506)
- Create `PartitionManagementTask` for scheduler to update accelerated table partition assignments by [@phillipleblanc](https://github.com/phillipleblanc) in [#9378](https://github.com/spiceai/spiceai/pull/9378)
- Distributed v1/sql query: Schedulers coordinate assigning partitions for executors, and for use at query time by [@phillipleblanc](https://github.com/phillipleblanc) in [#9325](https://github.com/spiceai/spiceai/pull/9325)
- feat: Change /v1/sql and FlightSQL to use local execution in cluster mode by [@phillipleblanc](https://github.com/phillipleblanc) in [#9055](https://github.com/spiceai/spiceai/pull/9055)
- Remove `DistributeFileScanOptimizer` and `UnionProjectionPushdownOptimizer` & set `target_partitions` dynamically based on cluster capacity by [@phillipleblanc](https://github.com/phillipleblanc) in [#9100](https://github.com/spiceai/spiceai/pull/9100)
- Add UDTF serialization support for distributed Ballista execution by [@phillipleblanc](https://github.com/phillipleblanc) in [#9200](https://github.com/spiceai/spiceai/pull/9200)
- Add Cluster Observability (Metrics+Dashboard) by [@peasee](https://github.com/peasee) in [#9066](https://github.com/spiceai/spiceai/pull/9066)
- feat(cluster): add executor control stream heartbeat by [@phillipleblanc](https://github.com/phillipleblanc) in [#9072](https://github.com/spiceai/spiceai/pull/9072)
- runtime: deregister executors on shutdown by [@phillipleblanc](https://github.com/phillipleblanc) in [#9302](https://github.com/spiceai/spiceai/pull/9302)
- cluster: reduce executor heartbeat timeout from 180s to 30s by [@phillipleblanc](https://github.com/phillipleblanc) in [#9300](https://github.com/spiceai/spiceai/pull/9300)
- cluster: route Ballista task cancellation via control stream by [@phillipleblanc](https://github.com/phillipleblanc) in [#9391](https://github.com/spiceai/spiceai/pull/9391)
- fix(cluster): rewrite table scans inside subqueries for distributed execution by [@phillipleblanc](https://github.com/phillipleblanc) in [#9518](https://github.com/spiceai/spiceai/pull/9518)
- fix(cluster): route non-partitioned accelerated tables in distributed mode by [@phillipleblanc](https://github.com/phillipleblanc) in [#9508](https://github.com/spiceai/spiceai/pull/9508)
- fix(cluster): tolerate Completed->status propagation race in distributed query handle by [@phillipleblanc](https://github.com/phillipleblanc) in [#9510](https://github.com/spiceai/spiceai/pull/9510)
- feat: Update DuckDB to 1.4.4 by [@phillipleblanc](https://github.com/phillipleblanc) in [#9252](https://github.com/spiceai/spiceai/pull/9252)
- feat: update delta_kernel to version 0.18.2 and refactor DeltaTable engine initialization by [@phillipleblanc](https://github.com/phillipleblanc) in [#9528](https://github.com/spiceai/spiceai/pull/9528)
- Upgrade mistral.rs and remove candle fork by [@phillipleblanc](https://github.com/phillipleblanc) in [#9284](https://github.com/spiceai/spiceai/pull/9284)
- Upgrade to Turso v0.4.4 by [@phillipleblanc](https://github.com/phillipleblanc) in [#9120](https://github.com/spiceai/spiceai/pull/9120)
- x.ai models now exclusively use /v1/responses endpoint by [@phillipleblanc](https://github.com/phillipleblanc) in [#9400](https://github.com/spiceai/spiceai/pull/9400)
- support chat template in HF by [@phillipleblanc](https://github.com/phillipleblanc) in [#9543](https://github.com/spiceai/spiceai/pull/9543)
- Add Databricks SQL dialect for DataFusion unparser by [@avishafir](https://github.com/avishafir) in [#9314](https://github.com/spiceai/spiceai/pull/9314)
- Snowflake data connector - add `snowflake_private_key` parameter by [@phillipleblanc](https://github.com/phillipleblanc) in [#9085](https://github.com/spiceai/spiceai/pull/9085)
- Add acceleration refresh ingestion metrics (rows_written, bytes_written) by [@peasee](https://github.com/peasee) in [#9461](https://github.com/spiceai/spiceai/pull/9461)
- Add `dataset_acceleration_size_bytes` metric by [@peasee](https://github.com/peasee) in [#9062](https://github.com/spiceai/spiceai/pull/9062)
- Enable core scalar UDFs in refresh SQL by [@phillipleblanc](https://github.com/phillipleblanc) in [#9502](https://github.com/spiceai/spiceai/pull/9502)
- Replace deprecated serde_yaml with internal yaml implementation by [@peasee](https://github.com/peasee) in [#9215](https://github.com/spiceai/spiceai/pull/9215)
- v1/search API to always return an array in matches by [@sgrebnov](https://github.com/sgrebnov) in [#9272](https://github.com/spiceai/spiceai/pull/9272)
- Replace deprecated claude-3-5-haiku-latest with claude-haiku-4-5 by [@phillipleblanc](https://github.com/phillipleblanc) in [#9492](https://github.com/spiceai/spiceai/pull/9492)
- Upgrade AWSK crates & set APN user-agent in AWS SDK credential bridge by [@lukekim](https://github.com/lukekim) in [#8328](https://github.com/spiceai/spiceai/pull/8328)
- Upgrade docx-rs to use zip 7.4.0 by [@phillipleblanc](https://github.com/phillipleblanc) in [#9392](https://github.com/spiceai/spiceai/pull/9392)
- feat: implement pretty printing for Arrow RecordBatches with data types by [@peasee](https://github.com/peasee) in [#9232](https://github.com/spiceai/spiceai/pull/9232)
- Fix Flight SQL schema consistency: expand view types and verify field names by [@phillipleblanc](https://github.com/phillipleblanc) in [#9438](https://github.com/spiceai/spiceai/pull/9438)
- Fix TLS connection for grpc+tls:// Flight SQL endpoints and add custom CA certificate support by [@phillipleblanc](https://github.com/phillipleblanc) in [#9073](https://github.com/spiceai/spiceai/pull/9073)
- Fix for `hash_index` with additional filters by [@krinart](https://github.com/krinart) in [#9385](https://github.com/spiceai/spiceai/pull/9385)
- fix: Preserve schema in results cache for empty query results by [@peasee](https://github.com/peasee) in [#9485](https://github.com/spiceai/spiceai/pull/9485)
- query: reconcile execution stream nullability with logical plan schema by [@phillipleblanc](https://github.com/phillipleblanc) in [#9486](https://github.com/spiceai/spiceai/pull/9486)
- fix: Handle schema evolution mismatch errors during data refresh by [@phillipleblanc](https://github.com/phillipleblanc) in [#9527](https://github.com/spiceai/spiceai/pull/9527)
- feat: Return dataset error message in datasets API by [@peasee](https://github.com/peasee) in [#9487](https://github.com/spiceai/spiceai/pull/9487)
- DuckDB (partition_mode: tables): rename partitioned_write_flush_threshold to partitioned_write_flush_threshold_rows by [@sgrebnov](https://github.com/sgrebnov) in [#9257](https://github.com/spiceai/spiceai/pull/9257)
- Make partition discovery more robust and make initialization non-blocking by [@sgrebnov](https://github.com/sgrebnov) in [#9499](https://github.com/spiceai/spiceai/pull/9499)
- feat(runtime): add runtime ready_state on_registration semantics by [@peasee](https://github.com/peasee) in [#9522](https://github.com/spiceai/spiceai/pull/9522)

**Full Changelog**: <https://github.com/spiceai/spiceai/compare/v1.11.2...v2.0.0-rc.1>
