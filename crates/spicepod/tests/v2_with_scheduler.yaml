version: v2
kind: Spicepod
name: v2_with_scheduler
runtime:
  scheduler:
    state_location: s3://my-bucket/scheduler-state
    partition_management:
      interval: 15s
      max_assignments_per_cycle: 50
      max_partitions_per_executor: 500
      discovery_timeout: 120s
